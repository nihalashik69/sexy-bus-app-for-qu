/// Maps configuration and constants
///
/// Central place for Google Maps related constants: default camera
/// positions, map styles, marker hues, and helper functions for marker
/// creation. Keeps map styling and constants out of widget files.

import 'package:google_maps_flutter/google_maps_flutter.dart';

class MapsConfig {
  static const double campusMarkerHue = BitmapDescriptor.hueGreen;
}
